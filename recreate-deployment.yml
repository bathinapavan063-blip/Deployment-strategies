apiVersion: apps/v1
kind: Deployment
metadata:
 name: recreate-nginx-deployment
 labels:
  name: web-app
  type: frontend
spec:
 template:
  metadata:
   name: nginx-pod
   labels:
    name: web-app
    type: frontend
  spec:
   containers:
   - name: nginx-container
     image: nginx:latest
 replicas: 30
 strategy:
  type: Recreate
 selector:
  matchLabels:
   name: web-app
   type: frontend

   
